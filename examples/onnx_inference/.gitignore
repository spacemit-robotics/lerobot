# 大型或可再生成文件，不提交到仓库。
# riscv64 ort sdk 目录。
cpp/spacemit-ort.riscv64.*/
# onnx / pytorch 权重。
models/
# cmake 构建目录。
build/
build_hw/
cpp/build/
cpp/build_*/
__pycache__/
*.pyc
debug/

# 测试输入，可通过 tools/make_act_test_inputs.py 重新生成。
cpp/inputs/*.npy
ref_*.npy

# models/ 已整体忽略。如需提交 act_norm_stats.txt，需要显式执行 git add -f。
